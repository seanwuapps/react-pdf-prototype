import React from 'react';
import logo from '../assets/logo.svg';

export default () => {
  return (
    <div className="header">
      <div class="logo">
        <img src={logo} />
      </div>
    </div>
  );
};
